{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "routes": [
    {
      "src": "/api/v1/(.*)",
      "dest": "${API_REWRITE_ORIGIN}/api/v1/$1",
      "env": ["API_REWRITE_ORIGIN"]
    },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
